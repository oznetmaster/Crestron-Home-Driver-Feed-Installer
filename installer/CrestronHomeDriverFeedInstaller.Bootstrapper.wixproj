<!--
  Copyright ©2026 Neil Colvin
  Licensed under the MIT License. See LICENSE file in the project root for full license information.
-->
<Project Sdk="WixToolset.Sdk/5.0.2">
  <PropertyGroup>
	<OutputType>Bundle</OutputType>
	<Platforms>AnyCPU;x64</Platforms>
	<Platform Condition="'$(Platform)' == ''">AnyCPU</Platform>
	<EnableDefaultItems>false</EnableDefaultItems>
	<OutputName>Crestron Home Driver Feed Installer</OutputName>
	<SetupProject>$(MSBuildThisFileDirectory)CrestronHomeDriverFeedInstaller.Setup.wixproj</SetupProject>
	<SetupMsiPath>$(MSBuildThisFileDirectory)bin\$(Configuration)\Crestron Home Driver Feed Installer.msi</SetupMsiPath>
	<DotNetDesktopRuntimeFileName>windowsdesktop-runtime-10.0.9-win-x64.exe</DotNetDesktopRuntimeFileName>
	<DotNetDesktopRuntimeUrl>https://builds.dotnet.microsoft.com/dotnet/WindowsDesktop/10.0.9/windowsdesktop-runtime-10.0.9-win-x64.exe</DotNetDesktopRuntimeUrl>
	<DotNetDesktopRuntimePath>$(MSBuildThisFileDirectory)$(DotNetDesktopRuntimeFileName)</DotNetDesktopRuntimePath>
	<DefineConstants>$(DefineConstants);SetupMsiPath=$(SetupMsiPath)</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
	<PackageReference Include="WixToolset.Bal.wixext" Version="5.0.2" />
	</ItemGroup>

  <ItemGroup>
	<Compile Include="Bundle.wxs" />
  </ItemGroup>

	<Target Name="DownloadDotNetDesktopRuntime" BeforeTargets="BeforeBuild" Condition="!Exists('$(DotNetDesktopRuntimePath)')">
		<DownloadFile SourceUrl="$(DotNetDesktopRuntimeUrl)" DestinationFile="$(DotNetDesktopRuntimePath)" />
	</Target>

  <Target Name="BuildSetupMsi" BeforeTargets="BeforeBuild">
	<Exec Command="dotnet build &quot;$(SetupProject)&quot; -c $(Configuration)" />
  </Target>
</Project>

<Window x:Class="NugetForCrestronDrivers.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="NugetForCrestronDrivers" Height="760" Width="1200" MinHeight="700" MinWidth="1100">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.2*" />
            <ColumnDefinition Width="1.1*" />
            <ColumnDefinition Width="1.1*" />
        </Grid.ColumnDefinitions>

        <GroupBox Header="NuGet feed" Grid.Row="0" Grid.Column="0" Margin="0,0,8,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="180" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Feed URL" Margin="0,0,0,4" />
                <TextBlock Text="Search" Grid.Row="1" Margin="0,8,0,4" />

                <TextBox Grid.Column="1" Grid.ColumnSpan="2" Margin="8,0,0,0" Text="{Binding FeedUrl, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="8,8,8,0" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="1" Grid.Column="2" Margin="0,8,0,0" Padding="12,4" Command="{Binding SearchCommand}" Content="Search" />
            </Grid>
        </GroupBox>

        <GroupBox Header="Processor connection" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" Margin="8,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Host" VerticalAlignment="Center" />
                <TextBox Grid.Column="1" Margin="8,0,12,8" Text="{Binding ProcessorHost, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Column="2" Text="Port" VerticalAlignment="Center" />
                <TextBox Grid.Column="3" Margin="8,0,0,8" Text="{Binding ProcessorPort, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="1" Text="Username" VerticalAlignment="Center" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="8,0,12,8" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Password" VerticalAlignment="Center" />
                <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="3" Margin="8,0,0,8" PasswordChanged="PasswordBox_OnPasswordChanged" />

                <CheckBox Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" IsChecked="{Binding RememberCredentials}" Content="Remember credentials for this processor" />
                <Button Grid.Row="2" Grid.Column="3" HorizontalAlignment="Right" Padding="12,4" Command="{Binding LoadCredentialsCommand}" Content="Load saved" />
            </Grid>
        </GroupBox>

        <GroupBox Header="Packages" Grid.Row="1" Grid.Column="0" Margin="0,0,8,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <DataGrid ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedPackage}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="2*" />
                        <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="*" />
                        <DataGridTextColumn Header="Authors" Binding="{Binding Authors}" Width="1.5*" />
                        <DataGridTextColumn Header="Downloads" Binding="{Binding DownloadCount}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Button Grid.Row="1" Margin="0,8,0,0" Padding="12,4" HorizontalAlignment="Left" Command="{Binding InspectCommand}" Content="Download and inspect selected package" />
            </Grid>
        </GroupBox>

        <GroupBox Header="Package contents" Grid.Row="1" Grid.Column="1" Margin="0,0,8,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Text="NuGet archive entries" Margin="0,0,0,8" FontWeight="SemiBold" />
                <TextBox Grid.Row="1" Text="{Binding PackageEntries}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap" IsReadOnly="True" />
            </Grid>
        </GroupBox>

        <GroupBox Header="drivers.json" Grid.Row="1" Grid.Column="2" Margin="0,0,0,8">
            <Grid Margin="8">
                <TextBox Text="{Binding DriversJsonContent}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" TextWrapping="NoWrap" IsReadOnly="True" />
            </Grid>
        </GroupBox>

        <DockPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
            <Button DockPanel.Dock="Right" Padding="16,6" Command="{Binding InstallCommand}" Content="Upload to processor" />
            <TextBlock VerticalAlignment="Center" Text="{Binding StatusMessage}" TextWrapping="Wrap" Margin="0,0,12,0" />
        </DockPanel>
    </Grid>
</Window>
